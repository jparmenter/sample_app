<%= link_to gravatar_for(@user, size: 52), @user %>
<h1>
  <%= @user.name %>
</h1>
<span><%= link_to "@#{@user.username}", @user, class: "text-muted" %></span>
<% if !current_page?(user_path(@user)) %>
  <span>
    <%= pluralize(@user.microposts.count, "micropost") %>
  </span>
<% end %>
